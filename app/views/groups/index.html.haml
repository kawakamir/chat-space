
=render 'menu'
.contents
  .side-bar
    -current_user.groups.each do |group|
      =render partial: 'group_boxes', locals: { group: group}

  .right-contents
    =render 'messages'
  .footer
    .boxes
      .text-box
        %p
          type a message
      .send-box
        %p
          Send
      %i.fa.fa-image

